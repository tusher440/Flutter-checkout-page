import 'package:flutter/material.dart';
class checkend extends StatefulWidget {
  const checkend({Key? key}) : super(key: key);

  @override
  _checkendState createState() => _checkendState();
}

class _checkendState extends State<checkend> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Check End"),
      ),
    );
  }
}
